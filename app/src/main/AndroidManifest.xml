<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">

        </activity>

        <activity android:name=".SplashActivity" android:label="splash Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activitys.TaskActivity"
            android:label="Test Activity" />
        <activity
            android:name=".activitys.AccountActivity"
            android:label="Account Activity" />
        <activity
            android:name=".activitys.TaskWindowActivity"
            android:label="EngTask Activity" />
        <activity
            android:name=".activitys.UpcomingActivity"
            android:label="Upcoming Activity" />

    </application>

</manifest>